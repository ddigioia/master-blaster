import React from 'react'
import { shallow } from 'enzyme'
import ReactDOM from 'react-dom'
import Board from './Board'

describe('attributes', () => {
  it('resets started flag', () => {
    // shallow(<Board />)
    // const board = new Board()
    // expect(board.started).toEqual(false)
  })
})